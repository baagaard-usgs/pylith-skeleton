# Notes

## Tests

1. What is the best way to compare `dimensional` objects in tests?
2. What is the best way to check a `uri` object in tests?

## Running tests for multiple components

I am getting errors when I run tests for multiple objects due to creation of multiple Pyre applications.
For each component, I am testing whether it gets configured correctly from a YAML file, so I create a Pyre application and instantiate it.

```shell
test_cartesian (test_geographic):
-- while registering component 'test_geographic', an instance of 'App':
-- another app, component 'test_cartesian', an instance of 'App', is already registered
test_geographic (test_geographiclocal):
-- while registering component 'test_geographiclocal', an instance of 'App':
-- another app, component 'test_geographic', an instance of 'App', is already registered
```

I think I want to create a clean configuration store before testing a component, setup the configuration store for just that component from a YAML file, and then reset the configuration store after the test of the component.

1. Is this the right idea?
2. If, so how can I implement this?

## YAML configuration for lists of components

```yaml
# This does not work, len(values) == 0. No error message.
# Default value is `analytic_values.basic`.
elastic_properties.values:
  - name: vp
    units: km/s
    expression: x
    coord_sys: cartesian
  - name: vs
    units: km/s
    expression: x
    coord_sys: cartesian

# Works
elastic_properties.values:
  - basic#vp
  - basic#vs

vp:
  name: vp
  units: km/s
  expression: x
  coord_sys: cartesian
vs:
  name: vs
  units: m/s
  expression: x + y
  coord_sys: geographic
```

1. I assume anytime I have a list of components, I need to give the components names. Is ths correct?
2. In this particular case, should I use a list of dicts instead of a list of components?

Anything that calls registerPackage and import package, then that NAME.yaml file will be read.
