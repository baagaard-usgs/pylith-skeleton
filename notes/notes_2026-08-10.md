# Notes

## PyLith

### foundry decorator

1. Default component for schema uses `fn`, not `fn()`, correct?

Correct.
Default value is foundry or component class or component instance.

Component instance (least useful example but can be right), shared default for all instantiations. Exactly like a default argument with a list (that is shared). Singleton = instance.

If not singleton, then need factory (foundry or component class).
Pyre takes default value. None, str (resolve), is instance of compatible component, if callable (class or foundry) the invokes callable (if instance, etc).

foundry - callable - instance of compatible compatible.

TODO: Test case with 3 examples.

2. Protocols should not use foundry in `__ini__.py`, correct?

Correct. Protocol.

pyre._default for favorite default. Does not expose import chain.

### List

1. Is it possible to specify a default value for a list?

`materials = pylith.properties.list(schema=elasticity_eqn.bulk_rheology(default=isotropic_linear))`

1. What exactly does `default=isotropic_linear` do?

Default list is empty. When instatiating a list, no mechanism to specify default.
Default for schema inside a list is meaningless.

default=None. Someone has to guarantee don't fall into trap. Don't want to think all modifying the same empty list. Force me to write code if None, then complain or ignore.

2. Can I set the default for `materials` to be `[isotropic_linear]`?

Need test to see if `default = [isotropic_linear]` creates a singleton.
Test with list of ints then with list of components.

### Debugging

```bash
 .. at /Users/baagaard/src/cig/pyre/packages/pyre/calc/Postprocessor.py:58
 .. in 'getValue'
 -- 'NoneType' object has no attribute 'pyre_tip' and no __dict__ for setting new attributes
*** journal.ext.journal.FirewallError: pyre.calc: FIREWALL BREACHED!
```

My guess is that an `import` statement or something similar is failing.
What strategies should I use to debug these types of errors?

Only happens to descriptor. Trait descriptor is none. Try to reduce test case to simple. While trait was being harvested, the machinery failed.

TODO: Create simple test case to debug.

1. How do I trap import errors at the proper spot to get an error message?

2. Is it possible to dump the configuration store in a meaningful structure/format?

`cli/Debug.py`

`qed debug nfs --root=(one of / qed journal pyre)`

Transverse tree of configuration store of trait values.
Rather than paint the tree, could send to YAML file.
Copy into cli.
If getting value values, try trapping error.
#
vfs - traverse configuration and print what is available.
`qed debug vfs`
