# Notes

## Michael's status

mm works out of the box for Ubuntu (dpkg), conda, macports, Fedora (rpm).
yum is not supported (replaced by `dnf`).

Windows support

- GitHub runner support
- Native Windows support for mm/pyre

mm Docker images

### Externals

Each dependency provides packages per distribution.
Marker for facilities to be optional (mark dependency as optional).

## Pyre grid

Improved build time (templates).

Write performance requires sequencing.
HDF5 writer in Pyre (layout of file; get raster).
Need to respect chunks (write one chunk).
Mosaic of chunks (prepared by application and generate collection of tiles).
Store in memory HDF5 tiles on disk.
Flush to disk. Not fully implemented. Will be implemented in pyre::grid.
Pyre interface to HDF5 will be more Pythonic than h5py.

## PyLith+mm for GUI

### PyLith GUI updates

- Version status implemented via GraphQL
- Cleaned up icons

- Stop
  - Provides link to PyLith port for restarting server.

### Port for web GUI

The PyLith web server always uses a different port.
Should I set things up to use a consistent port? If so, how?

Freeze port using configuration. Pick a port.

# overall application configuration

```yaml
qed.app:
    # run as a web app
    # Setting default shell to web; would break `pylith schema` (need to use --shell=script in .mm/pylith.ux when generating schema).
    #shell: web

    # for the web server
    nexus.services.web:
        # pin the port for serving web content to some number in user space
        address: ip4:0.0.0.0:8005
 
# configure the {web} shell
pyre.shells.web#qed.app.shell:
    # spawn a new browser window/tab every time the server starts?
    auto: no
```

### Debugging GraphQL stuff

1. How do I use the Python debugger with `pylith --shell=web`?

Michael will send VSCode fragment to attach to server.

2. How do I debug the Javascript side? Dev tools?

React tools. Extension page search for React Tools.
In addition to the DOM facilities. React icon will show component hierarchy.

Turn on element picker. Part of page where problem is. Examine structure, fonts, etc.

### Styling component

qed- style mixing by hand.

Package `style-components`. Fingernail with nail polish.
Grep qed for styling.

Cost is each component instance creates a new style entry in memory stylesheet.
Some hex string. Apply to single component.
Another library does style as compile time. Michael found it annoying. Requires naming convention.

:TODO: Get latest mm and pyre.

Verify mm without conda.