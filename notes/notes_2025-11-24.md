# Notes

1. Review PyLith application parameter settings.
2. Review updated code layout.
   1. Governing equations
   2. Protocols
3. Trapping import errors.

      ```console
      pyre.calc (firewall):
      .. at /Users/baagaard/src/cig/pyre-1.x/packages/pyre/calc/Postprocessor.py:62
      .. in 'getValue'
      -- 'NoneType' object has no attribute 'pyre_tip'```
      ```

   Error is coming from `ModuleNotFound` or `ImportError`   

4. Fix use of SolverPetsc.pyre_initialized() to validate solver settings.
5. What are the pros/cons of using `schema(default=X)` versus providing `pyre_default()` in the protocol?
6. CIG nemesis: PySys_SetArgv() is deprecated in Python v3.11
7. How to run Pyre tests?
8. Does Pyre+MPI work on Brad's computer?
   1. What error(s) are generated?
