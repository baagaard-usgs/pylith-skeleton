# Notes

## Updates

- Converted current PyLith to use predefined set of journal channels.
- Fixed CIG pythia/pyre installation so color-console works.
- Created PyLith Exception objects; show C++ backtrace.
  - Broad set of base classes and semantic meaningful base class (enough context to generate useful information)
    - qed - multiple layers, decoration, catching, sending information
  - Suggestions for integrating exceptions with journal?
    - Error (user) versus Firewall (code needs to be changed)
    - journal is log book and channels are sections of log book; meaningful sections, importance.

## Michael's status / estimates of delivery

- mm (status)
  - TODO: Schedule an mm meeting
  - Auto discover location of packages conda, user, macports, dpkg (controlled `--pkgdb=conda`; find all packages that mm supports)
  - Build mode (conda mode: installs inside conda environment `--mode=conda` `--mode=ubuntu` need sudo because writes to system; useful for docker; --mode=`dev` requires prefix and buildroot, target, machine
  - --branch in dev mode fold branch and repo into target. Isolate build from others.
    - Creates environment variables
    - `mm --branch=off`
  - Support for pytest, catch2, gtest, playwright (headless browser)
- pyre (requested features / issues)
  - pyre_showConfiguration() traversal into list of components. Issues 136, 170.
  - MPI integration; Issues 159, 160, 168.
  - Report suspected configuration errors. Issue 137.
  - Write configuration to YAML
    - Configure app programmatically and then save configuration to YAML
    - Edit existing YAML and save (preserve comments) Ruamel YAML
  - SEND THIS LIST TO MICHAEL

## Building

- Will likely experiment with adding mm and CMake support for pylith-skeleton
  - Pyre conda recipe (very similar to PyLith)

## Aside - marimo notebooks

- Python code with decorators.
- Runs as Python script or notebook (web app).
- Considering creating examples using marimo notebooks

## GUI for parameters

Want to start thinking about design.

### Python versus JavaScript

- React.js (JavaScript)
  - Upside: Work Claude can do
  - Pyre: semantic markups - agent can understand what GUI is doing
- NiceGUI, Streamlit, etc (Python -> JavaScript)
  - Could be options

### Steps

Question: What should be loaded? Configuration files? Output of pyre_showConfiguration()? If configuration files, then need to load them on the fly?

qed loads configuration. Can load multiple configuration files. Has reset() (back to when server started).

Sandbox where configurable components come from. User actively opts in for injecting code.
home/.pyre/pylith (foo.py is searchable with sufficient context; pylith installation directory, working directory, home directory)

GUI-
Sketch out screens. Mechanics of wiring are straightforward. 
Wizard, settings page, tree, filter, activity bar, task bar (based on activity), data window
Pyre template with server+client with window; single activity (documentation)
  qed source code; markdown component
webpack but much better solutions now (vite); graphql serializes requests and responses; local state management (make changes - relay)

1. Viewer (no ability to change values)
2. Editor (change properties with validation)
3. Editor (swap components; user specifies path to component, with validation)
4. Editor (swap components; user selects from available components implementing protocol)
5. Monitor simulation
6. Steering simulation
