# Notes

## Updates

- Setup micromamba on my laptop.
- Converted to use of foundry decorator (with help from free GitHub Copilot).
- Implemented more tests of components (with help from free GitHub Copilot).
  - Python test scripts must have different names.
  - Copilot didn't always implement all cases (directories, traits) even when it said it did.

## shells/cli directory structure

Currently, I have

```bash
- bin/pylith
- packages/pylith
  - cli
    - __init__.py # uses @foundry
    - About.py
    - Config.py
    - Debug.py
    - Help.py
    - Info.py
    - Run.py
  - shells
    - __init__.py # Does NOT use @foundry
    - Action.py
    - Command.py
    - Plexus.py
```

```
- packages/pylith
  - apps
    - ux (daemon that listens to network for events; event loop)
    - gql (graphql resolvers); Marshaller for encoding commands from one endpoint (serializers them) and transmit to receiver and decoded intocommands (like RPC). Transmits and receives JSON objects. Allows requestor what subset of data to return (want fields); do it for object graph. Server can evolve independently of client. Relay helps synchronizes remote view with client's view of data model and generate events. Relay is GraphQL for React.
    - cli
      - __init__.py # uses @foundry
      - About.py
      - Config.py
      - Debug.py
      - Help.py
      - Info.py
      - Run.py
    - shells
      - __init__.py # Does NOT use @foundry
      - Action.py
      - Command.py
      - Plexus.py
```

Journal demon - would use graphql instead of pickle.

1. Is there a better layout?

No longer using apps/PyLithApp. Replaced by cli/Run.py.

2. Should I use @foundry in shells/__init__.py. How would the use of shells look in `bin/pylith`?

:TODO: Open issue to make foundry work like component class record.
:TODO: Annoyed by needing to implement Action and Command (to implement action) in shells (just to set family).

Does Python allow passing family name to protocol contructor?

## Multiple applications

May need both Script and Plexus in shells directory to support multiple drivers.

## Next steps

- Implement more components and fill in missing pieces of existing components.



## Environment modules setup with mm

mm_targets
mm_compilers
mm_prefix
mm_buildroot
mm_packages

load compilers/clang
load targets/opt


mm_copmiler_fuiew
mm_compiler_suite_version
mm_compilers


mm/mm.tcl

create vars for build directories and install prefix

.config/mm use environment variables