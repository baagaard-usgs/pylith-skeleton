# Notes

These are issues in Pyre that are related to PyLith development.

## Updates to journal constructors for consistency across Python, pybind11, C++

- `Make.mmm` in `~/.config/pyre/mm.yaml`! To avoid conflicts with legacy Make.mm in tests.
- Remove update to Make.mm (last 2 commits).
- Check for consistency between Make.mm and actual tests (targets).
- See setter for dent.

## MPICH integration

- `make/extern/mpi/init.mm` missing mpich flavor for `mpi.libraries`
- Fail gracefully if macports is not installed.
- Use of --hostfile in tests (and any other non-standard MPI command line arguments)

### `init.mm`


TODO: Michael turn on black and clang-format in VSCode workspace settings. Issue created.

TODO: Reimplement MPI bindings in pybind11. Issue created.
MPI_COMM_WORLD is immortal. Cannot let reference count go to 0.

MPI package
port.py: private send/recv channel between two processes (rank 0 commnication between two communicators)
Could offload checkpointing to a different communicator.

TODO (long term): Revive journal daemon to receive journal messages (serializes) so never get garbled output. Issue created.

packages/mpi/__init__.py

ImportError: create a journal warning


merlin (pyre application for build) uses these settings for building.
```
mpi.shells.mpirun.mpi: pyre.externals.mpi.openmpi#brad
brad:
    launcher: mpiexec
    defines:
    bindir: 
    incdir:
    libdir:
    libraries: [list of libraries on link line]
```