# Notes

How do I setup VS code to see Python virtual environment from mm.branch?

Look at .vscode/c_.json
vs_prefix_usr
vs_prefix_sysecho $mm_

VSCode seems to be failing to find conda environment


## Pyre: List of ints or components

Added tests for list[int] and list[componen].

- No default -> [] (correct)
- default=None -> None (correct)
- default=[5, 3, 9] -> [5, 3, 9] (correct, not singleton)

Behavior for list[component] is also correct.

- TODO: Add test for default factory `pyre.properties.list(schema=PROTOCOL(default=MYCLASS))`.

Questions

- Create MR with tests? TODO


## Implemented `pylith debug nfs` and `pylith debug vfs`

1. Need help understanding `nfs` output?
    - How is `nfs` (filesystem) connected to the parameters that are listed?
      - Was using filesystem or Hierarchical,
      - Can name anything we want.
    - Why do I get `pylith.app` and `pylith` information? `pylith-app` or `pylithapp`
2. Need help understanding `vfs` output.
    - How do I create a vfs?
    - How do I add stuff to a vfs?

/pylith

Configuration need to be files on filesystem.
Magic location where pylith is installed.
etc/pylith/elasticity/foo.py --elasticity=foo [attached to installation directory]
~/.config/pyre/elasticity/foobar.py for user components

Pyre builds a vfs where / is a non-existent filesystem location.
Can mount parts of filesystem on vfs.
Access is orthogonal to location of source.

User case - project. Mount data using vfs.

Application `pyre_mount` create nodes. Extra layer to shape where get information from. User mounts their local to "standard" location for major pieces.

Grab node and mount. Mount zip files. Mount S3. Mount Earth Access (CRM catalog).

## Updates from Michael

### Pyre

journal - can go to GUI
qed - new button to connect to server and get log (client gets journal)
multiple browsers to same server (can watch agent interact with server)

