# Notes

## Michael's status

- qed (install status)

## pyre.reset() in pyre/__init__.py

Create a PR? :TODO: Create branch `issue171`.

## mm setup

- Have very basic Python package
  - `mm tests` appears to run all of the tests using pytest
  - How do I run tests in a directory?
    - Workaround: run pytest
    - Add pytest as a first class test
- How do I add support for an additional external package? Can I do this within pylith or does this have to be done in mm?

Files go in `PACAKGE/.mm/extern`

## C++ stacktrace

Are there better options than cpptrace to get a human readable backtrace in C++ for C++14 - C++20?

c++23 has backtrace and templates concepts.
CI GitHub runners - getting correct version of c++ runtime library has been problematic.
Recommends moving to C++23 to get these features.

## Clusters

Install micromamba.
~/.config/mm to configure packages from system
user@machine overrides what is discovered by conda
Version 5 of OpenMPI understands almost all current MPI transport mechanisms
libhdf5 - needs to point to correct MPI + C

Could create small test application leveraging MPI+Python

mm --show
mm --verbose



## GUI for parameters

- Design notes in `notes/gui-design.md`.
- Raw code (nonfunctional) in `gql` and `ux` directories under `packages/pylith/apps/`
